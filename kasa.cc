#include <bits/stdc++.h>
using namespace std;

// nazwa biletu, cena, długość działąnia
using ticket = pair<string, pair<int, int>>;
// id przystanku, czas zatrzymania
using stop = pair<int, int>;

// stops - the first parameter is the id of the stop, the second
// one is the time of the stop, whis is number of minuts from 00:00
void add_route(int route_id, vector<stop> stops) {

}

void add_ticket(ticket t) {

}

// para - <przystanek, numer kursu>
// ostatnia para ma numer kursu = 0
string query(vector <pair<int, int> > q) {

}

int main() {

}